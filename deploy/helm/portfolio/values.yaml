replicaCount: 1

image:
  repository: ogabek0331/devops-portfolio
  tag: latest
  pullPolicy: Always


service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: nginx
  host: portfolio.local
  path: /
